const roles = [
  `transaction.create`,
  `transaction.update`, 
  `transaction.delete`,
  `transaction.read`, 
  `banner.create`,
  `banner.update`,
  `banner.delete`,
  `banner.read`,
]

const userRoles = [
  'transaction.delete',
  'transaction.read', 
  'banner.create',
  'banner.update',
]

const isUserAllowedToAccess = () => {
  //

  return true //
}